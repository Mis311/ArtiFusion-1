// import { Chip } from '@mui/material';

// const TagList = ({ tags, onTagClick }) => {
//   return (
//     <div>
//       {tags.map((props) => (
//         <Chip key={props.tag} label={props.tag} size="small" sx={{ mr: 1 }} onClick={() => onTagClick(props.tag)} />
//       ))}
//     </div>
//   );
// };

// export default TagList;
